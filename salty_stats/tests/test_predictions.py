from salty_stats import db
from salty_stats.tests.base import BaseTestCase
from salty_stats.predictor import predict_winner


class TestPredictions(BaseTestCase):
    #def test_all_predictions(self):
    #    all_characters = self.session.query(db.Character).all()
    #
    #    for p1 in all_characters:
    #        for p2 in all_characters:
    #            predict_winner(self.session, p1, p2)
    pass
